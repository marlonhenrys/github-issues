// usar isso no helper fetch (criar nova função)
const { stackOverflow } = require('../config/request')

module.exports = async params => {
    console.log(params, stackOverflow)

    // criar nova função para posts nos helpers 'format' e 'file' 
    // (usar o params.filename - o valor dele é 'posts')
}